import { Meta, Typeset } from '@storybook/blocks'

<Meta title="Styles / Typography" />

export const typography = {
  type: {
    sans: '"Plus Jakarta Sans", sans-serif'
  },
  weight: {
    normal: '400',
    semibold: '600',
    bold: '700'
  },
  size: {
    xs: 10,
    sm: 14,
    base: 16,
    xl: 20,
    '2xl': 24
  }
}

export const SampleText =
  'Lorem ipsum dolor sit amet, consectetur adipiscing elit.'

# Typography

**Sizes:** text-xs (10px), text-sm (14px), text-base (16px), text-xl (20px), text-2xl (24px)

**Font:** Plus Jakarta Sans (text-sans)

## Weights

### 400 (font-normal)

<Typeset
  fontSizes={Object.values(typography.size)}
  fontWeight={typography.weight.normal}
  sampleText={SampleText}
  fontFamily={typography.type.sans}
/>

### 600 (font-semibold)

<Typeset
  fontSizes={Object.values(typography.size)}
  fontWeight={typography.weight.semibold}
  sampleText={SampleText}
  fontFamily={typography.type.sans}
/>

### 700 (font-bold)

<Typeset
  fontSizes={Object.values(typography.size)}
  fontWeight={typography.weight.bold}
  sampleText={SampleText}
  fontFamily={typography.type.sans}
/>
